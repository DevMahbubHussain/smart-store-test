/**
 * Product Title Block Save Component
 * 
 * Based on WooCommerce's product-title save component
 * Returns null to force PHP rendering on frontend
 */

import SaveNull from '../../shared/saveNull.jsx';

export const Save = SaveNull;
