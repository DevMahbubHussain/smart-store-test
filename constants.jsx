/**
 * Product Title Block Constants
 */

import { heading, Icon } from '@wordpress/icons';

export const BLOCK_ICON = (
	<Icon icon={heading} className="wpsp-block-editor-components-block-icon" />
);
